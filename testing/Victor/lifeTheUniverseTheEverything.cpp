#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(nullptr);
    ios_base::sync_with_stdio(0);
    int x;
    cin >> x;
    
    while(x != 42) {
        cout << x << endl;
        cin >> x;
    }

    return 0;
}

